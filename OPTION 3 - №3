#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    setlocale(0,"");

    double a,y;

    cout << "a = ";
    cin >> a;

    if (a < -2)
    {
        y = -a - 2;
    }
    else
    {
        if (a <= 2)
        {
            y = 2 - a;
        }
        else
        {
            y = a - 2;
        }
    }

    cout << "F(a) = " << y;

    return 0;
}
